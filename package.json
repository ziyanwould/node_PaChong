{
  "name": "ziyanwould",
  "version": "1.0.0",
  "description": "爬虫及ES7等的运用",
  "main": "爬HTML.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon -w src --exec \"babel-node src --presets env\"",
    "build":"rimraf dist && babel src -s -D -d dist --presets env"
  },
  "author": "紫烟would",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.3.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "babel-register": "^6.26.0",
    "co": "^4.6.0",
    "nodemon": "^1.18.10",
    "rimraf": "^2.6.3"
  }
}
